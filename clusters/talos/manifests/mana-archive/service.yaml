apiVersion: v1
kind: Service
metadata:
  name: mana-archive
  namespace: cnpg-system
  labels:
    app: mana-archive
spec:
  type: NodePort
  selector:
    app: mana-archive
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080
      protocol: TCP

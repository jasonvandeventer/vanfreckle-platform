apiVersion: v1
kind: Service
metadata:
  name: cartarch
  namespace: cnpg-system
  labels:
    app: cartarch
spec:
  type: NodePort
  selector:
    app: cartarch
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080
      protocol: TCP

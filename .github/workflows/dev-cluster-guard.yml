name: dev-cluster-guard
on:
  pull_request:
  push:
    branches: [main]
jobs:
  no-archiver:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: dev cluster must never archive to prod backup path
        run: bash ci/check-dev-no-archiving.sh
